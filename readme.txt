Read Me.
#. this website is about Employee Management System
# Containts two links Emoployee Lists and Register where it opens its own tab
# we have a search system, you can search using name, department
# We have hardcoded Employee lists currently in a table
# We have a employee registration section. where we can add new employees.
We have a delelete button in the table that allows us to delete employees.


#key technologies used.

1. Html 
    basic tags like divs, classes, sections, <a> etc
2. CSs for styling
    @media queries for smaller or larger screen
    dsiplay flex properties
    flex-box properties
    and other various stylings like backgroud color, border, paddings etc
3. Java scripts 
    Java explicityly to add or remove a employee the from the lists
    Mains tools used were:
        DOMContentLoaded
            so that content insde DOMContentLoaded loads only after the other codes are executed
        addEventListener
            it listens for clicked button, submited, delete,
        JSON.parse(localStorage.getItem()) 
            converts text to strings format that code can understands
        localStorage.setItem('', JSON.stringify())
            converts code to text
        appendChild
            to show executed result on the screen.
